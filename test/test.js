/* global describe, it */

'use strict';

const assert = require('assert');
const oniyiVcardParser = require('../');

describe('oniyi-vcard-parser node module', () => {
  it('must have at least one test', () => {
    oniyiVcardParser();
    assert(false, 'I was too lazy to write any tests. Shame on me.');
  });
});
